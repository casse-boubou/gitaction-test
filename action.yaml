name: "Docker Check"
author: "XXXXXXXXXXXX"
description: "XXXXXXXXXXXX"
inputs:
  dockerfile:
    description: "Path for the Dockerfile to analyse"
    required: true
    default: ""
branding:
  # icon: "terminal"
  # color: "gray-dark"
  icon: 'code'
  color: 'yellow'
runs:
  using: "composite"
  steps:
    - uses: actions/setup-python@v7
      name: Install python
      with:
        python-version: '3.14'

    - name: Install python requirements
      shell: bash
      run: |
        python -m pip install --upgrade pip
        python -m pip install -r "${{ github.action_path }}/src/requirements.txt"

    - name: Run the check
      id: check
      shell: bash
      env:
        DOCKERFILE: ${{ inputs.dockerfile }}
      run: python3 "${{ github.action_path }}/src/main.py" "$GITHUB_WORKSPACE/${{ inputs.dockerfile }}"

    - name: Exit action
      shell: bash
      run: exit ${{steps.check.outputs.statuscode}}
